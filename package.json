{
  "name": "aplan",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/api",
    "packages/web"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace packages/shared",
    "dev": "npm run build:shared && concurrently -n shared,api,web -c blue,green,magenta \"npm:dev --workspace packages/shared\" \"npm:dev --workspace packages/api\" \"npm:dev --workspace packages/web\"",
    "build": "npm run build:shared && npm run build --workspace packages/api && npm run build --workspace packages/web",
    "test": "npm run test --workspace packages/api",
    "seed": "npm run seed --workspace packages/api"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "allowScripts": {
    "@prisma/client@5.22.0": true,
    "@prisma/engines@5.22.0": true,
    "bcrypt@5.1.1": true,
    "esbuild@0.28.1": true,
    "esbuild@0.21.5": true,
    "prisma@5.22.0": true
  }
}

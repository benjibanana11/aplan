# Copiez ce fichier en ".env" et remplissez les valeurs (jamais commité, voir .gitignore).

# URL de base de l'API backend. En local, laissez tel quel (valeur par défaut du code si absent).
# En production sur Render, injectée automatiquement (voir render.yaml).
VITE_API_URL="http://localhost:4000"
